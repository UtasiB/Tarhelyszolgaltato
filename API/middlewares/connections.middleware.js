const User = require('../models/user.model').User;
const Storage = require('../models/storage_package.model').Storage;
const Subscription = require('../models/subscription.model').Subscription;

module.exports = () => {
    try{
       

      
    }
    catch(err){
        console.log(err);
    }
}
// Define associations between Subscription and Storage
;

// Define associations between Subscription and User
